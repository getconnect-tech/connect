enum TeamSize {
    SMALL
    MEDIUM
    LARGE
    ENTERPRISE
}

enum PriorityLevels {
    NONE
    LOW
    MEDIUM
    HIGH
    URGENT
}

enum MessageType {
    REGULAR
    EMAIL
    CHANGE_PRIORITY
    CHANGE_ASSIGNEE
    CHANGE_LABEL
    CHANGE_STATUS
    FROM_CONTACT
}

enum TicketSource {
    MAIL
    WEB
}

enum TicketStatus {
    OPEN
    CLOSED
    SNOOZE
}

enum UserRole {
    MEMBER
    ADMIN
    OWNER
}

enum ConfigType {
    CHANNEL
}
