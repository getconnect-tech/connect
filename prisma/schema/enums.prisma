enum TeamSize {
  SMALL
  MEDIUM
  LARGE
  ENTERPRISE
}

enum PriorityLevels {
  NONE
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum MessageType {
  REGULAR
  EMAIL
  CHANGE_PRIORITY
  CHANGE_ASSIGNEE
  CHANGE_LABEL
  CHANGE_STATUS
  FROM_CONTACT
}

enum ChannelType {
  MAIL
  WEB
  INTERNAL
}

enum TicketStatus {
  OPEN
  CLOSED
}

enum UserRole {
  MEMBER
  ADMIN
  OWNER
}

enum EmailEventType {
  OPENED
  BOUNCED
  DELIVERED
  LINK_CLICKED
  SPAMED
  FAILED
}

enum GENDER {
  MALE
  FEMALE
}
