enum TeamSize {
    SMALL
    MEDIUM
    LARGE
    ENTERPRISE
}

enum PriorityLevels {
    NONE
    LOW
    MEDIUM
    HIGH
    URGENT
}

enum MessageType {
    REGULAR
    EMAIL
    PRIORITY
    ASSIGN
}

enum TicketSource {
    MAIL
    WEB
}

enum TicketStatus {
    OPEN
    CLOSED
    SNOOZE
}
